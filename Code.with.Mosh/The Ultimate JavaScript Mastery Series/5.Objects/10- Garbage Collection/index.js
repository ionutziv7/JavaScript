let circle = {};
console.log(circle);

// Garbage Collector finds the variables or constants that are no longer 
// used and then deallocate the memory that was allocate to them earlier
// Memory allocation and deallocation happends automatically
